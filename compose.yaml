services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: zero-ttt:torch2.13.0-cuda13.2-cudnn9-devel
    working_dir: /workspace
    gpus: all
    shm_size: 8gb
    init: true
    stdin_open: true
    tty: true
    volumes:
      - type: bind
        source: .
        target: /workspace
      - huggingface-cache:/cache/huggingface
      - pip-cache:/cache/pip
      - torch-cache:/cache/torch
      - torch-extensions-cache:/cache/torch_extensions
      - uv-cache:/cache/uv
    environment:
      PIP_CACHE_DIR: /cache/pip
      PYTHONPATH: /workspace/src
    command: ["/bin/bash"]

  training-agent:
    build:
      context: .
      dockerfile: Dockerfile
    image: zero-ttt:torch2.13.0-cuda13.2-cudnn9-devel
    working_dir: /workspace
    gpus: all
    shm_size: 8gb
    init: true
    volumes:
      - type: bind
        source: ./configs/profiles
        target: /profiles
        read_only: true
      - type: bind
        source: ./runs
        target: /runs
      - type: bind
        source: ${ZERO_TTT_DATA_ROOT:-D:/datasets/Zero-TTT}/raw
        target: /datasets/raw
        read_only: true
      - type: bind
        source: ${ZERO_TTT_DATA_ROOT:-D:/datasets/Zero-TTT}/staging
        target: /datasets/staging
      - type: bind
        source: ${ZERO_TTT_DATA_ROOT:-D:/datasets/Zero-TTT}/manifests
        target: /datasets/manifests
      - type: bind
        source: ${ZERO_TTT_DATA_ROOT:-D:/datasets/Zero-TTT}/processed
        target: /datasets/processed
      - type: bind
        source: ${ZERO_TTT_DATA_ROOT:-D:/datasets/Zero-TTT}/catalog
        target: /datasets/catalog
      - huggingface-cache:/cache/huggingface
      - pip-cache:/cache/pip
      - torch-cache:/cache/torch
      - torch-extensions-cache:/cache/torch_extensions
      - uv-cache:/cache/uv
    environment:
      PIP_CACHE_DIR: /cache/pip
      PYTHONPATH: /workspace/src
      ZERO_TTT_SOURCE_ROOT: /datasets
      ZERO_TTT_STAGING_ROOT: /datasets/staging
      ZERO_TTT_MANIFEST_ROOT: /datasets/manifests
      ZERO_TTT_CATALOG_PATH: /datasets/catalog/catalog.sqlite
      ZERO_TTT_STORE_ROOT: /datasets/processed
      ZERO_TTT_RUN_ROOT: /runs
      ZERO_TTT_PROFILE_ROOT: /profiles
      ZERO_TTT_AGENT_HOST: 0.0.0.0
      ZERO_TTT_AGENT_PORT: "8090"
    command: ["zero-ttt-agent"]
    expose:
      - "8090"
    stop_grace_period: 30m
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://127.0.0.1:8090/api/v2/healthz"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 5s

  tensorboard:
    build:
      context: .
      dockerfile: Dockerfile
    image: zero-ttt:torch2.13.0-cuda13.2-cudnn9-devel
    working_dir: /workspace
    init: true
    volumes:
      - type: bind
        source: ./runs
        target: /runs
        read_only: true
    command:
      - tensorboard
      - --logdir
      - /runs
      - --host
      - 0.0.0.0
      - --port
      - "6006"
      - --reload_interval
      - "5"
    ports:
      - "127.0.0.1:6006:6006"
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://127.0.0.1:6006/"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s

  training-ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: zero-ttt:torch2.13.0-cuda13.2-cudnn9-devel
    working_dir: /workspace
    init: true
    environment:
      PYTHONPATH: /workspace/src
      ZERO_TTT_AGENT_URL: http://training-agent:8090
      ZERO_TTT_TENSORBOARD_URL: http://127.0.0.1:6006
      ZERO_TTT_UI_HOST: 0.0.0.0
      ZERO_TTT_UI_PORT: "8080"
    command: ["zero-ttt-ui"]
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      training-agent:
        condition: service_healthy
      tensorboard:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://127.0.0.1:8080/"]
      interval: 5s
      timeout: 3s
      retries: 12
      start_period: 10s

  katago-version:
    profiles: ["katago"]
    build:
      context: .
      dockerfile: docker/katago/Dockerfile
    image: zero-ttt/katago:v1.17.2-cuda13.2
    command: ["version"]

  katago-analysis:
    profiles: ["katago"]
    build:
      context: .
      dockerfile: docker/katago/Dockerfile
    image: zero-ttt/katago:v1.17.2-cuda13.2
    gpus: all
    init: true
    stdin_open: true
    volumes:
      - type: bind
        source: ./models/katago
        target: /models
        read_only: true
      - type: bind
        source: ./configs/katago
        target: /config
        read_only: true
    command:
      - analysis
      - -model
      - /models/${KATAGO_MODEL_FILE:-model.bin.gz}
      - -config
      - /config/analysis.cfg

  katago-gtp:
    profiles: ["katago"]
    build:
      context: .
      dockerfile: docker/katago/Dockerfile
    image: zero-ttt/katago:v1.17.2-cuda13.2
    gpus: all
    init: true
    stdin_open: true
    tty: true
    volumes:
      - type: bind
        source: ./models/katago
        target: /models
        read_only: true
      - type: bind
        source: ./configs/katago
        target: /config
        read_only: true
    command:
      - gtp
      - -model
      - /models/${KATAGO_MODEL_FILE:-model.bin.gz}
      - -config
      - /config/gtp.cfg

volumes:
  huggingface-cache:
  pip-cache:
  torch-cache:
  torch-extensions-cache:
  uv-cache:
